<Application x:Class="Rovo.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="Ink" Color="#182C39" />
        <SolidColorBrush x:Key="MutedInk" Color="#526571" />
        <SolidColorBrush x:Key="Accent" Color="#146B62" />
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource Ink}" />
            <Setter Property="Background" Value="#F5F7F8" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="BorderBrush" Value="#A7B5BE" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="18,9" />
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#A7B5BE" />
        </Style>
        <Style TargetType="Label">
            <Setter Property="Padding" Value="0,0,0,6" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8" />
            <Setter Property="MinHeight" Value="38" />
        </Style>
    </Application.Resources>
</Application>
